# atomemo-plugin-sdk-ex - 部署运行与配置

## 1. 集成方式
- 作为 Hex/Git 依赖被插件项目引入。
- 不作为独立 HTTP 服务部署。

## 2. 配置分组
- Runtime 模式（debug/release topic）。
- Hub 连接参数。
- 文件/LLM 上下文能力开关与超时策略。

## 3. 发布建议
- 采用语义化版本，破坏性变更必须有迁移说明。
- 与 `atomemo-elixir-plugins` 联动做兼容测试矩阵。
